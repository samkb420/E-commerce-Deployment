apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: apps-ingress
spec:
  rules:
  - host: sam.com
    http:
      paths:
      - backend:
          serviceName: auth
          servicePort: 50
      - backend:
          serviceName: product
          servicePort: 80
      - backend:
          serviceName: order
          servicePort: 90
